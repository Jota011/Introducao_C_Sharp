//Video Aula 5: https://youtu.be/f8rDk6nJ9DE

using System;

class Aula05
{
    static void Main(string[] args)
    {
        //Operadores: 
        // + 
        // - 
        // * 
        // / 
        // % 
        // ++ 
        // --

        //Operadores de atribuição: 
        // =    (atribuição)
        // +=   (adição e atribuição)
        // -=   (subtração e atribuição)
        // *=   (multiplicação e atribuição)
        // /=   (divisão e atribuição)
        // %=   (módulo e atribuição)

        //Operadores de comparação:
        // ==   (igualdade)
        // !=   (diferença)
        // >    (maior que)
        // <    (menor que)
        // >=   (maior ou igual a)
        // <=   (menor ou igual a)

        //Operadores lógicos:
        // && (E)
        // || (OU)
        // ! (NÃO)

        //Operadores de concatenação:
        // + (string)  

        //Operadores de incremento e decremento:
        // ++ (incremento)
        // -- (decremento)

        //Operadores de atribuição compostos:
        // += (adição)
        // -= (subtração)
        // *= (multiplicação)
        // /= (divisão)
        // %= (módulo)

        //Operadores de identidade:
        // is (verifica se é do tipo)
        // as (converte para o tipo)

        //Operadores de null-coalescing:
        // ?? (retorna o valor à esquerda se não for nulo, caso contrário retorna o valor à direita)

        //Operadores de tipo:
        // typeof (obtém o tipo de uma variável)

        //Operadores de interpolação de strings:
        // $ (permite incluir expressões dentro de strings)

        //Operadores de range:
        // .. (define um intervalo de valores)

        //Operadores de indexação:
        // [] (acessa elementos de uma coleção)

        //Operadores de atribuição de deslocamento:
        // <<= (desloca bits para a esquerda e atribui)
        // >>= (desloca bits para a direita e atribui)

        //Operadores de atribuição de bit a bit:
        // &= (opera AND bit a bit e atribui)
        // |= (opera OR bit a bit e atribui)

        //Operadores de atribuição de XOR bit a bit:
        // ^= (opera XOR bit a bit e atribui)

        //Operadores de atribuição de deslocamento circular:
        // << (desloca bits para a esquerda)
        // >> (desloca bits para a direita)
        // >>> (desloca bits para a direita sem sinal)
        // <<< (desloca bits para a esquerda sem sinal)
        
        //Operadores de atribuição de concatenação:
        // += (concatena strings e atribui)
        int res = 10 + 5;
        Console.WriteLine(res);
        
    }
}